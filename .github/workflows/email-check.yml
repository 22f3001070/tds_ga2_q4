name: Email Check Workflow

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: 22f3001070@ds.study.iitm.ac.in
        run: echo "Hello, world!"

      - name: Final Step
        run: echo "Workflow completed successfully"
